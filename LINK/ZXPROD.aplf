 C←A ZXPROD B;⎕IO;D
⍝ COMPUTES THE ENTRY-BY-ENTRY PRODUCT OF TWO ARRAYS
⍝ OF INTEGER POLYNOMIALS.
 →NOTEST/BEGIN
 DERR∧/(,A=⌊A),,B=⌊B
 EXPANDV
BEGIN:⎕IO←0
 C←(A∘.×(¯1↑⍴B)⍴1)×((⍳¯1+⍴⍴A),0 ¯1+⍴⍴A)⍉B∘.×(¯1↑⍴A)⍴1
 C←C,((⍴A),¯1+¯1↑⍴A)⍴0
 C←+/[¯2+⍴⍴C]((⍴A)⍴-⍳¯1↑⍴A)⌽C
 D←1⌈⌈/,+/∨\⌽C≠0
 C←((¯1↓⍴C),D)↑C
